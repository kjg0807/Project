<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    


<mapper namespace="com.iu.home.category.CategoryDAO">

	<insert id="setAdd" parameterType="CategoryDTO">
		INSERT INTO CATEGORY VALUES (CATEGORY_SEQ.NEXTVAL,#{categoryName})
	</insert>
	<select id="getList" resultType="CategoryDTO">
		SELECT CATEGORYNAME FROM CATEGORY
	</select>
	<!-- <select id="getCategoryList" resultType="ShopDTO" resultMap="getCategoryListResult"> 
		SELECT C.*, S.* FROM CATEGORY C LEFT JOIN SHOP S ON C.CATEGORYNUM = S.CATEGORYNUM 
		WHERE S.CATEGORYNUM = #{categoryNum} </select> <resultMap type="CategoryDTO" 
		id="getCategoryListResult"> <id column="CATEGORYNUM" property="categoryNum"/> 
		<result column="CATEGORYNAME" property="categoryName"/> <association property="ShopDTO" 
		javaType="List"> <id column="SHOPNUM" property="shopNum"/> <result column="CATEGORYNUM" 
		property="categoryNum"/> <result column="SHOPPHONE" property="shopPhone"/> 
		<result column="SHOPNAME" property="shopName"/> <result column="SHOPADDRESS" 
		property="shopAddress"/> <result column="PRICEAVG" property="priceAvg"/> 
		<result column="DELIVERY" property="delivery"/> <result column="CONTENTS" 
		property="contents"/> <result column="TITLE" property="title"/> </association> 
		</resultMap> -->
	<delete id="setDelete" parameterType="CategoryDTO">
		DELETE CATEGORY WHERE CATEGORYNUM=#{categoryNum}
	</delete>
</mapper>